import { getBall } from "../../3d/3d.js";

export function mooveBall(ball)
{
	const ball = getBall();
// Create the ball mesh
if	(object === "BALL")
{	
	ball.position.x = data.BALL.x;
	ball.position.y = data.BALL.y;
	ball.position.z = data.BALL.z;
}
}
